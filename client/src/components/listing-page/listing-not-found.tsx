const ListingNotFound = () => {
  return (
    <div className="mt-10">
      Article not found, the owner might have removed the item or the item has
      been sold.
    </div>
  );
};

export default ListingNotFound;
