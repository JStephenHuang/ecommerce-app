const SearchBar = () => {
  return (
    <input
      className="w-[40%] h-[50%] px-[1rem] border border-black"
      type="text"
      placeholder="Search uniforms and sellers"
    />
  );
};

export default SearchBar;
