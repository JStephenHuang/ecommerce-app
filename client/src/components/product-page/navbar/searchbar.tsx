const SearchBar = () => {
  return (
    <input
      className="w-full h-[50%] rounded-md px-[1rem] border border-black"
      type="text"
      placeholder="Search uniforms and sellers"
    />
  );
};

export default SearchBar;
