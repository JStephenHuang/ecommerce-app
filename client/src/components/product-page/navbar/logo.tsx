import { Link } from "react-router-dom";

const Logo = () => {
  return (
    <Link className="text-[28px] mr-[3rem] font-bold italic" to="/">
      Off the Brand
    </Link>
  );
};

export default Logo;
