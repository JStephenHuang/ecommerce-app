import { Link } from "react-router-dom";

const Logo = () => {
  return (
    <Link className="w-[30%] text-[28px] font-bold italic" to="/">
      Off the Brand
    </Link>
  );
};

export default Logo;
