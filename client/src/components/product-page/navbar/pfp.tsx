import { Link } from "react-router-dom";

const ProfilePic = () => {
  return (
    <Link
      className="h-[24px] w-[24px] rounded-full bg-[#912F56]"
      to="/profile"
    ></Link>
  );
};

export default ProfilePic;
